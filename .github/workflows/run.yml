name: Nasdaq AI Bot Realtime

on:
  schedule:
    # 한국 시간 기준 평일(월-금) 실행
    - cron: '0 21 * * 1-5'   # 한국 오전 6:00 (마감 분석)
    - cron: '30 14 * * 1-5'  # 한국 오후 11:30 (개장 분석)
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10' # 3.9에서 3.10으로 업그레이드!

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # 최신 버전 호환성을 위해 라이브러리 설치 최적화
          pip install yfinance pandas requests pytz google-generativeai

      - name: Run AI Auto Trader
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python main.py

